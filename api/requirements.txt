# Runtime deps for the Vercel Python function ONLY (api/index.py).
# Deliberately minimal -- no torch / transformers / matplotlib / dbt / streamlit.
# The full local dev/eval stack is in the repo-root requirements.txt.
duckdb==1.1.3
sqlglot>=25.0,<26
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.31
fastapi>=0.110
pydantic>=2.6
